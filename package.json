{
  "name": "sunrise-management-system",
  "version": "1.0.0",
  "description": "Sunrise Keepers Managementment System Parent Folder",
  "engines": {
    "node": "13.7.0",
    "npm": "6.13.6"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NPM_CONFIG_PRODUCTION=false cd backend && npm install && node backend/server.js && npm run build"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0",
    "node-sass": "^4.14.1"
  }
}
