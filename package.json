{
  "name": "sunrise-management-system",
  "version": "1.0.0",
  "description": "Sunrise Keepers Managementment System Parent Folder",
  "engines": {
    "node": "13.7.0",
    "npm": "6.13.6"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm run build",
    "install-build-client": "cd client && npm install && npm run build",
    "install-backend": "cd backend && npm install",
    "start": "node backend/server.js",
    "client": "cd client && npm start",
    "current-directory": "npm install",
    "dev": "concurrently \"npm start\" \"npm run client\"",
    "heroku-postbuild": "npm install && npm run install-backend && npm run install-build-client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.3.0",
    "node-sass": "^4.14.1"
  }
}
